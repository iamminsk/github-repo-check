export const Card = (props) => (
  <div
    css={{
      borderRadius: 10,
      flexBasis: "100%",
      backgroundColor: "rgba(255, 255, 255, 0.6)",
      padding: 30,
    }}
    {...props}
  />
);
